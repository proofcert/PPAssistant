module inter.
accumulate interact.
accumulate lkf-kernel.
interaction "A -> A"
  ((n (g a)) !-! (p (g a)))
%  ((all x\ (n (g x))) !-! (some x\ (p (g x))))
%  (interact L [u (u e)] _ e).
%  (interact L [] C e).
  (interact L CM CH e).
